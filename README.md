# koa-bones

## Install
```
brew cask install docker
# then run Docker app

git clone https://github.com/itswil/koa-bones.git
cd koa-bones

# for docker-compose to run in development mode (nodemon)
cp .env.example .env

docker-compose up
```
